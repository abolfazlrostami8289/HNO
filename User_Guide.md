# 🚀 راهنمای جامع نصب و استفاده از همیار نجات

سلام! به دنیای **همیار نجات** خوش آمدید.

همیار نجات یک دستیار هوشمند، کاملاً خصوصی و بدون نیاز به اینترنت (آفلاین) است که برای کمک به شما طراحی شده است. ما تمام تلاش خود را کرده‌ایم تا نصب و استفاده از این برنامه برای شما بسیار ساده و لذت‌بخش باشد. لطفاً این راهنما را قدم به قدم دنبال کنید. 💡

---

## 💻 پیش‌نیازها

برای اجرای همیار نجات، سیستم شما باید دارای شرایط زیر باشد:
- **سیستم عامل:** ویندوز ۱۰ (Windows 10) یا ویندوز ۱۱ (Windows 11)
- **فضای ذخیره‌سازی:** حداقل ۱۵ گیگابایت فضای خالی در هارد دیسک

---

## ⚠️ نکات بسیار مهم قبل از نصب

برای جلوگیری از هرگونه مشکل در حین نصب، لطفاً به این نکته بسیار مهم توجه کنید:

**پوشه نصب برنامه به هیچ وجه نباید دارای حروف فارسی یا فاصله باشد.**
- ❌ **نادرست:** `C:\سیستم\برنامه ها\HamyarNejat`
- ❌ **نادرست:** `D:\پوشه جدید\Hamyar Nejat`
- ✅ **درست:** `C:\HamyarNejat`
- ✅ **درست:** `D:\HamyarNejat`

توصیه می‌کنیم فایل فشرده (ZIP) دانلود شده را مستقیماً در درایو `C:\` یا `D:\` از حالت فشرده خارج کنید.

---

## 🛠️ مراحل نصب

نصب همیار نجات بسیار ساده و خودکار است. فقط کافی است مراحل زیر را دنبال کنید:

۱. فایل فشرده (ZIP) دانلود شده را به طور کامل از حالت فشرده خارج کنید (Extract).
۲. وارد پوشه استخراج شده شوید.
۳. **توجه:** به هیچ وجه فایل `install.ps1` را مستقیماً اجرا نکنید.
۴. برای شروع نصب، روی فایل **`RunMe.bat`** دو بار کلیک کنید.
۵. یک پنجره جدید باز می‌شود که مراحل نصب را به شما نشان می‌دهد. این فرآیند ممکن است کمی زمان ببرد. لطفاً صبور باشید و منتظر بمانید تا نصب کاملاً تمام شود (هیچ نیازی به اینترنت نیست).

---

## 💬 نحوه استفاده

تبریک می‌گوییم! همیار نجات اکنون روی سیستم شما نصب شده است. 🎉

برای شروع چت و استفاده از دستیار هوشمند خود:
- به صفحه اصلی ویندوز (Desktop) بروید.
- یک میان‌بر (Shortcut) با نام **"همیار نجات"** مشاهده خواهید کرد.
- روی آن دو بار کلیک کنید. برنامه به طور خودکار در مرورگر اینترنت شما باز می‌شود و می‌توانید به راحتی با آن گفتگو کنید.

---

## 🛑 نحوه بستن برنامه

**نکته بسیار مهم:** فقط بستن پنجره مرورگر باعث توقف کامل برنامه نمی‌شود و همیار نجات همچنان از منابع سیستم شما استفاده خواهد کرد.

برای بستن کامل و توقف برنامه:
- به صفحه اصلی ویندوز (Desktop) بروید.
- روی میان‌بر **"توقف همیار نجات"** (Hamyar Nejat - Stop) دو بار کلیک کنید.
- با این کار، دستیار هوشمند به طور کامل خاموش می‌شود.

---

## 🛠️ عیب‌یابی اولیه

اگر در حین نصب با مشکلی مواجه شدید، نگران نباشید:
- در صورت بروز خطا، پنجره نصب باز می‌ماند تا پیام خطا را به شما نشان دهد.
- به پوشه برنامه بروید و وارد پوشه `logs` شوید.
- فایلی به نام `install_debug.log` را پیدا کنید و آن را برای تیم پشتیبانی ما ارسال کنید تا در اسرع وقت مشکل شما را بررسی و حل کنند. 📞

از استفاده از **همیار نجات** لذت ببرید! 🌟
